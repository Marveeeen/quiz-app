import logoImg from "../assets/quiz-complete.png";

const Header = () => {
  return (
    <header>
      <img src={logoImg} alt="app logo" />
      <h1>Quiz App</h1>
    </header>
  );
};

export default Header;
